# Railway/Nixpacks configuration for Floteris
# This monorepo deploys the backend API by default

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm install"]

[start]
cmd = "cd backend && npm start"

[variables]
NODE_ENV = "production"
